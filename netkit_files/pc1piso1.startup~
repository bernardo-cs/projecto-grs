/sbin/ifconfig eth0 up
/sbin/ifconfig eth0 hw ether 00:00:00:00:01:00
dhclient

#ver lista de utilizadores
#cat /etc/passwd | cut -d":" -f1      

#adicionar utilizador, depois seguir os passos... utilizador a adicionar: bernardo pc1 e guilherme pc2
#adduser bernardo
useradd -m -g users -p sa3tHJ3/KuYvI -d /home/bernardo -s /bin/sh bernardo

su bernardo

#username="bernardo"
#password="password"
#pass=$(perl -e 'print crypt($ARGV[0], "password")' $password)
#echo $pass
#useradd -m -p $pass $username
